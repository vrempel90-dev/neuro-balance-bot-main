name: Claude Repair Dashboard Contract

on:
  pull_request:
    paths:
      - "repair_app.py"
      - "repair_status.py"
      - "repair_service.py"
      - "tests/test_repair_status.py"
      - ".github/workflows/repair-dashboard-contract.yml"
  push:
    branches: [main]
    paths:
      - "repair_app.py"
      - "repair_status.py"
      - "repair_service.py"
      - "tests/test_repair_status.py"
      - ".github/workflows/repair-dashboard-contract.yml"

permissions:
  contents: read

jobs:
  dashboard-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest
      - name: Compile dashboard modules
        run: python -m py_compile repair_status.py repair_app.py repair_service.py
      - name: Run dashboard regression tests
        run: python -m pytest -q tests/test_repair_status.py
